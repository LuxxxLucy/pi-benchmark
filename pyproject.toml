[project]
name = "pi-benchmark"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.40",
    "torch>=2.0",
    "datasets>=2.0",
    "pandas",
    "tabulate",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
